#lab3-6
# 1. 创建数据框（输入题目给定的数据）
df <- data.frame(
  运营商 = c(189, 135, 188, 134, 138),
  地区 = c(2225, 2225, 2225, 2225, 2225),
  号码段 = c(4812, 5003, 9938, 6753, 9313)
)

# 2. 使用 paste() 合并三列，生成新列：电话号码
# sep = "" 表示合并时不加任何符号，直接拼接
df$电话号码 <- paste(df$运营商, df$地区, df$号码段, sep = "")

# 3. 查看最终结果
df