#lab1-4
# 将数据写入文本文件 (students.txt)



# 创建学生数据
students <- data.frame(
  姓名 = c("张三", "李四", "王五", "赵六", "丁一"),
  性别 = c("女", "男", "女", "男", "女"),
  年龄 = c(14, 15, 16, 14, 15),
  `身高/cm` = c(156, 165, 157, 162, 159),   # 加上反引号 `` 防止特殊字符报错
  `体重/kg` = c(42.0, 49.0, 41.5, 52.0, 45.5), # 允许列名包含特殊符号（如斜杠），不自动转换
  check.names = FALSE 
)

print(students)
